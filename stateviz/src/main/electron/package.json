{
	"name": "electron",
	"private": true,
	"version": "0.0.0",
	"main": "main.js",
	"scripts": {
		"start": "electron .",
		"format": "prettier . --write --ignore-unknown"
	},
	"devDependencies": {
		"prettier": "^2.8.8"
	},
	"dependencies": {
		"electron": "^24.2.0",
		"kill-port": "^2.0.1",
		"tree-kill": "^1.2.2"
	}
}
